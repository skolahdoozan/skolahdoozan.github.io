let name1 = 'Pac-Man';
console.log(`${name1} evades ghosts on boards of dots and fruit`)

let inky = {
    name: 'Inky',
    color: 'blue'
};

let pinky ={
    name: 'pinky',
    color: 'pink'
};

let blinky ={
    name: 'blinky',
    color: 'red'
};

let clyde ={
    name: 'clyde',
    color:'orange'
};

console.log(`${inky.name} is ${inky.color}`);

let values = [1,2,3,4,5];

let group2Teams =[];

group2Teams.push('t alpha');
group2Teams.push('t beta');
group2Teams.push('t gamma');
group2Teams.push('t delta');
group2Teams.push('t epsilon');
group2Teams.push('t zeta');
group2Teams.push('t eta');

console.log(values);

console.log(group2Teams);

for (var i = 0; i<group2Teams.length; i++)
{
    console.log(group2Teams[i]);
}
